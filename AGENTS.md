# Wedding Tales — agent handover

Read this before touching anything under `src/lib/salesAgent/` or
`src/lib/social/`. It is the state of a long build, written down so the
next session does not start from zero.

The commit messages in this repo are unusually long on purpose. They
explain *why* a thing is the way it is, including several decisions that
look wrong until you know what broke. `git log --oneline -15` then read
the ones that touch what you are about to change.

---

## The WhatsApp sales agent — shipped and live

A Claude-powered salesperson that answers WhatsApp on the business
number, qualifies, sends the demo and payment links, follows up, and
hands over to Lord when it should not answer.

**Shape.** Make is a dumb pipe; all logic is in this repo.
WhatsApp Cloud (Watch Events) → Make → `POST /api/sales-agent/reply` →
`{ sendText, sendImage, notifyOwner }` → Make sends it back.
Make scenario `9630287` on eu2.make.com, **Active**.

**Files.**
- `catalog.js` — the ONLY facts the agent may state. Prices, links,
  photos. Change a price here and the bot changes.
- `prompt.js` — assembles the system prompt from catalog + journey +
  experiment arm + what the CRM knows about this lead.
- `journey.js` — a written brief per funnel stage. Only the CURRENT
  stage is injected. This is the file to edit when a conversation goes
  badly; it is fixable the same afternoon, unlike the A/B test.
- `experiments.js` — four opening variants, assigned by phone hash.
  Refuses to name a winner until both leading arms clear 30 leads AND
  the gap beats the noise. Do not weaken that.
- `agent.js` — model call, JSON parsing that fails toward handoff, and
  `sanitizeReply()` which strips em dashes, markdown and surplus emoji.
- `leadsCore.js` — the PURE half of the CRM. Anything testable lives
  here, because `leads.js` boots firebase-admin and cannot be imported
  by a test.
- `leads.js` — Firestore `sales_leads/{phone}`.
- `digest.js` — the morning summary.

**Three ways the bot shuts up.** A human replying in the chat (detected
via Meta's echo, told apart from our own sends by comparing text to the
last assistant turn), an existing customer writing in, and the owner
muting it from his phone. All in `reply/route.js`, in that order, before
the model is ever called.

**Spend is metered here, not read from a dashboard.** `pricing.js` holds
the published rates; every model call is costed from the `usage` block it
returned and rolled into `sales_usage/{date}` plus a `_totals` document.
That buys cost per lead and cost per closed deal, which no provider
dashboard can give. Two things it is not, both said on screen: it counts
only what this app spends since counting began, and the rates are
hardcoded, so a price change makes it wrong until someone edits the file.
`RATES_CHECKED_ON` is the honesty marker.

**Follow-ups chase on a widening ladder, not a timer.** `followupPolicy.js`
owns every timing decision: 1 / 3 / 7 days, floors per stage so a fresh
quote gets air, scaled by how close the event is, and stopped dead once
the event has passed. Three attempts, the last one explicitly written as
a goodbye rather than a nudge. Quiet hours are Israel's: nothing before
09:00 or after 21:00, nothing Friday afternoon, nothing on Saturday at
all. `/api/sales-agent/followups` composes them; something external has
to call it. Outside Meta's 24h window only an approved TEMPLATE can be
sent - `withinWindow` on each item says which case it is, and ignoring
that is why follow-up automations appear to work and then silently stop.

**Admin.** `/admin/sales-leads` — triage strip, transcript, per-lead
actions, the experiment panel, and a button that sweeps the synthetic
`9725000009xx` test leads.

**Owner commands** from Lord's own number (needs `SALES_AGENT_OWNER_PHONE`):
`שקט <phone>`, `בוט <phone>`, `סטטוס <phone>`, `דוח`.

**Make sends broken JSON, and that is handled here.** The HTTP module
builds the body by interpolating values into a raw string, so a newline
or a quote in a customer's message makes it stop being JSON. That cost a
real lead on 8 August: two-line message, 400, no reply, no alert.
`inbound.js` repairs it by finding the known keys and taking the values
between them verbatim. The tidier fix is `toJSON()` in Make, still not
applied because it cannot be verified without a live message and it
fails loudly in the wrong direction. If it is ever applied, `inbound.js`
costs nothing - valid JSON never reaches the repair path.

**Env:** `ANTHROPIC_API_KEY`, `SALES_AGENT_SECRET`, `SALES_AGENT_OWNER_PHONE`,
`CRON_SECRET`, `OPENAI_API_KEY`, optional `OPENAI_IMAGE_MODEL`. Secrets are Lord's to enter — do not type them into forms.

**Open:** the daily digest push needs a WhatsApp template `wt_daily_digest`
(UTILITY, Hebrew, 4 single-line variables) because free-form business-
initiated messages die outside Meta's 24h window. `דוח` works today
without it. The same window limit applies to the handoff alerts.

---

## Social — in progress, NOT wired to any account

Nothing has ever been published. Instagram is `weddingtales.il`.

- `social/contentPlan.js` — six post angles on a deterministic rotation,
  each paired with a photo of a book we actually printed. **Done, tested.**
- `social/compose.js` — programmatic Hebrew overlay via satori + sharp.
  **Working, but Lord has asked to drop this approach**: he wants the
  image model to produce the picture with the caption baked in.
- `social/imagePrompt.js` — builds the request to gpt-image-1. Refuses
  to ask for a caption it expects to come back broken (too long, mixed
  script, digits, more than one line) and asks for a wordless picture
  instead. **Done, tested.**
- `/admin/social-preview` + `/api/social/preview` — the four test
  renders, one per request because four in a single serverless
  invocation times out and returns nothing at all. **Built, never run**:
  it needs `OPENAI_API_KEY` in Vercel, which is Lord's to add.

**Before deleting compose.js, know what it cost to learn.** Satori does
not implement the Unicode bidi algorithm, so Hebrew rendered fully
reversed until `toVisualOrder()` was added; the Latin wordmark rendered
as empty boxes until a Latin font subset was registered under its own
family name. Multi-line RTL headlines still wrap in the wrong order
(wrap first in logical order, then reorder per line — not done). If the
image model turns out to mangle Hebrew, this file is the fallback and
those three lessons are why it works.

**Next:** open `/admin/social-preview`, press the button, look at the
four pictures. If the Hebrew is clean the caption stays inside the
image; if it breaks, the caption moves under the picture and the images
go out wordless. That one look decides the architecture and nothing
after it should be built before it. Then: caption writing, an approval
queue (he approves before publish — agreed, at least for the first
month), and a Make scenario publishing via Make's own approved IG/FB
connectors, so no Meta App Review is needed.

---

## Working in this repo

`.git/index.lock` gets orphaned because the Cowork bridge cannot delete
files. Any plain `git` command through `device_bash` leaves one behind
and blocks VS Code. Always set `GIT_INDEX_FILE` to a path in `/tmp`, and
write `.git/refs/heads/main` directly instead of `git commit`.

Tests: `npx vitest run tests/`. The sales agent and social suites are
pure by design — keep them that way.

## שלא ייפול בין הכיסאות (sweep.js)

הסולם ב-`followupPolicy.js` מטפל רק בליד שיש לו `followUpAt`. הבעיה
האמיתית היא ליד שאיבד את השדה הזה: כתיבה שנכשלה אחרי שהתשובה כבר
יצאה, סטטוס שנערך ידנית בטבלה, או handoff שפג. אף אחד מהם לא זורק
שגיאה, ואף אחד מהם לא מופיע בשום לוג — והתוצאה תמיד זהה: מישהו שאל
על ספר ברכות ולא שמע יותר כלום.

`src/lib/salesAgent/sweep.js` מפריד שני מקרים שדורשים טיפול הפוך:

- **orphan** — ליד חי בלי צעד הבא, לא מסומן human, פחות מ-3 ניסיונות,
  והמגע האחרון לפני יותר מ-36 שעות. חוזר לסולם אוטומטית
  (`reviveOrphans` ב-`leads.js` כותב `followUpAt: today` + `revivedCount`).
- **stale handoff** — מישהו סימן שצריך בן אדם, וה-48 שעות עברו. הבוט
  **לא** ממשיך את אלה לעולם. הן עולות ל-Lord כרשימה (`handoffAlert`),
  כי handoff קורה כשמישהו כועס או ביקש אדם, ובוט שמחכה יומיים ואז חוזר
  לשיחה בעליזות גרוע מבוט שלא העביר מלכתחילה.

`revivedCount` שווה מבט מדי פעם: אם אותם לידים ניצלים שבוע אחרי שבוע,
משהו למעלה מפיל כתיבות ורק המונה הזה יראה את זה.

### הריצה היומית

`/api/sales-agent/followups` עושה עכשיו שלושה דברים בסדר הזה: sweep
(מחזיר orphans לתור באותה ריצה), chase (כותב פולו-אפ אמיתי לכל מי
שבתור), escalate (מחזיר `alert` ל-Lord על handoffs תקועים, ו-`null`
ביום נקי — התראה שמגיעה כל בוקר עם 0 היא התראה שמפסיקים לקרוא).

מכבד את `sendableNow()`: בשבת ומחוץ ל-09:00–21:00 הראוט מחזיר
`{ok:true, skipped:'shabbat'}` ולא שולח כלום. הלידים נשארים due כי
`dueFollowUps` משווה ב-`<=`, אז שבת מדולגת הופכת לשליחה בראשון בבוקר.

מי קורא לזה: Vercel cron ב-`vercel.json`, `30 7 * * *` UTC = 10:30
שעון קיץ / 09:30 שעון חורף. עד עכשיו המנוע היה קיים ואף אחד לא קרא לו,
וזה היה הפער האמיתי.

`?dry=1` מרכיב הכל בלי לסמן כלום כנשלח ובלי להחיות אף ליד, ומתעלם
משעות שקטות — הנקודה שלו היא להסתכל.

## מה הוסר מהסופר-אדמין

Lord ביקש להעיף שלושה מסכים שלא היו רלוונטיים יותר:

- `/admin/landing` + `/api/admin/landing` — עורך דף הנחיתה השיווקי.
- `/admin/analytics` + `/api/admin/analytics` — "סטטיסטיקה חיה".
- `/admin/pipeline` — "לוח הפקה", ואיתו `/admin/board` (גרסה חדשה
  יותר של אותו לוח, שהייתה נגישה רק מקישור בתוך pipeline והייתה
  הופכת למסך יתום).

מה שנשאר וחשוב לדעת:

`site_config/landing` **עדיין נקרא** על ידי `/landing` הציבורי. הדף
ממשיך להיראות בדיוק כמו שהוא, עם מה שנשמר בפעם האחרונה — פשוט כבר אין
מסך שכותב לשם. שינוי בדף הנחיתה מעכשיו הוא שינוי בקוד.

`productionStatus` על מסמך wedding עדיין נכתב מעורך האירוע ב-`/admin`.
רק הלוח שהציג אותו נעלם, לא השדה.

`/api/admin/wedding-stats` ו-`/api/admin/stats-tokens` **נשארו** — הם
משרתים את פאנל האירוע הבודד ואת דף הסטטיסטיקה לזוג, לא את המסך שהוסר.

הקבצים לא נמחקו מהדיסק אלא הועברו ל-`_to_delete/` (ה-mount של OneDrive
חוסם unlink), והתיקייה ב-gitignore. מבחינת git הם מחוקים. אפשר למחוק
את התיקייה ביד.

### ונוסף

כפתור **תוכן לרשתות** בתפריט העליון → `/admin/social-preview`. המסך
מייצר את הפוסטים ומראה מה חזר; הוא לא מעלה לאינסטגרם בעצמו עדיין.

## הבוט: מחיר, מדיה, ולמידה

### "לורד" יצא ללקוחות
היה קשיח בשני מקומות — ההודעה שנשלחת ללקוח קיים, והשורה שמסבירה שאפשר
לקבל בן אדם. עכשיו `BUSINESS.ownerName` מ-`process.env.SALES_AGENT_OWNER_NAME`,
ובלי הגדרה נופל ל"מישהו מהצוות" / "הצוות". יש טסט שנכשל אם המילה חוזרת
לפרומפט.

### מחיר
הכלל של Lord: מי ששאל מחיר מקבל מחיר, בלי לקדם שאלות לפני. שלושה
שינויים:
1. `selling.js` — `SELLING_CRAFT`, המדריך שאומר את זה בפירוש, יחד עם
   קריאת מה שהצד השני באמת רוצה לשמוע, קידום השיחה, וסגירה.
2. שלב 0 ב"מהלך המכירה" בפרומפט, לפני כל שאר הסדר.
3. רשת ביטחון דטרמיניסטית: `priceDodged(incoming, messages)` בודק אם
   הלקוח שאל מחיר והתשובה חזרה בלי מספר, ואם כן `reply/route.js`
   מוסיף הודעה שנייה עם המחירים מהקטלוג. הפרומפט אומר את זה בארבעה
   מקומות ותחת לחץ המודל עדיין מתחמק, אז זה לא עוד פסקה.

### ספריית מדיה
`sales_media` ב-Firestore + `mediaLibrary.js` (טהור, נבדק). שש התמונות
ב-`catalog.js` נשארות כרצפה, וכל מה שמעלים מצטרף אליהן. המודל רואה
רשימה אחת ולא יודע מה מאיפה.

העלאה: הדפדפן מעלה ישירות ל-Firebase Storage תחת `sales-media/` ורק
ה-URL נשלח ל-API. סרטון 16MB דרך פונקציה של Vercel נתקל בתקרת 4.5MB
ונופל כ-413 אטום אחרי שהמשתמש כבר חיכה להעלאה כולה.

מגבלות וואטסאפ נאכפות בשלושה מקומות (UI, API, storage.rules): תמונה עד
5MB JPEG/PNG, סרטון עד 16MB MP4.

השדה `when` הוא היחיד שהמודל קורא כהוראה, ולכן הוא חובה — נכס בלי הסבר
מתי לשלוח אותו הוא נכס שלא נשלח או שנשלח לאדם הלא נכון.

סרטון מגיע ב-`sendVideo` / `sendVideoCaption` / `hasVideo` נפרדים,
כי זה מודול אחר בוואטסאפ. **צריך להוסיף מודול וידאו ב-Make** — עד אז
`hasVideo` פשוט תמיד false ושום דבר לא נשבר.

### מדידה
שני מונים על כל נכס: `replied` (הלקוח כתב בחזרה תוך 24 שעות משליחה)
ו-`won` (השיחה נסגרה בעסקה). ה-win מיוחס לכל מה שנשלח בשיחה, לא
לאחרון — last-touch כאן היה נותן את הקרדיט למה שנשלח הכי קרוב לקישור
התשלום.

מתחת ל-8 שליחות המסך מציג ספירה ומסרב להציג אחוז, ו-`performanceNote`
לא נכנס לפרומפט בכלל. אחוז מענה של 100% על שתי שליחות הוא המספר הכי
מטעה שדשבורד יכול להדפיס, וההחלטה שהוא מייצר — למחוק משהו שעבד — לא
הפיכה. גם כשיש מספיק, הניסוח לפרומפט הוא "זה מידע, לא הוראה", כי כלל
שגובר על רלוונטיות ישלח ספר חתונה למישהי ששואלת על בת מצווה.

### תוקן אגב
`isFinalAttempt` היה קיים ואף אחד לא העביר אותו — כל פולו-אפ שלישי
נכתב כעוד תזכורת במקום כפרידה.

### שלוש התמונות שהבוט שולח הכי הרבה

Lord שלח שלוש והן ראשונות ב-`MEDIA` — הסדר הוא משקל, המודל קורא מלמעלה:

- `upload_screen` — מסך העלאת הברכה בתוך טלפון. הנכס היחיד שמראה את
  המוצר מהצד של האורח, על מסך, שזה בדיוק איפה שהוא יפגוש אותו. עונה
  על "זה מסובך לאורחים?" / "צריך אפליקציה?".
- `cover_personalised` — כריכה אמיתית עם שם ותמונה. עונה על "זה באמת
  יהיה שלנו?" מהר יותר מכל משפט על התאמה אישית.
- `book_open_spread` — ספר פתוח. עונה על ההתלבטות שיושבת בדיוק בין
  החבילה הזולה לזו שמשלמת: "המודפס שווה את ההפרש?".

הפרומפט שונה במכוון לכיוון של **לשלוח יותר**. הנוסח הקודם אמר "תמונה
אחת מספיקה, שתיים זה כבר הרבה"; עכשיו הוא אומר ששתיים או שלוש במקומות
הנכונים זו שיחה טובה. עדיין אסור בהודעה הראשונה, ואסור אותה מדיה
פעמיים.

`tests/salesMediaFiles.test.js` הולך על כל URL ב-`MEDIA` ובודק שהקובץ
באמת קיים תחת `public/`. אף שלב אחר בשרשרת לא בודק את זה — המודל בוחר
מפתח, הראוט מעביר URL ל-Make, ווואטסאפ היא הראשונה שמסתכלת. קובץ חסר
מגיע ללקוח כהודעה בלי תמונה, בשקט. בדיוק זה קרה פעם עם ה-webp של
הסושיאל.

ושינוי קטן בלוגיקה: סטטיסטיקה מוצגת עכשיו לפי **אם יש נתונים**, לא לפי
מאיפה הנכס בא. המונים ממילא נכתבים לפי מפתח ולא אכפת להם אם הוא מהקוד
או מהעלאה, אז שלוש התמונות האלה נמדדות מהיום הראשון.

## מספרי עמודים בניהול ברכות

`src/lib/bookPageIndex.js` — מחשב לאיזה עמוד בספר המודפס כל ברכה נופלת,
ומציג את זה כתגית על הכרטיסייה ובשורת הסידור מחדש.

**למה זה לא טריוויאלי:** מיקום הכרטיסייה הוא לא מספר העמוד. ברכה ארוכה
עם תמונה מתפצלת לשני עמודים (`autoSplit` או `forceSplit` פר-ברכה),
`entriesPerPage: 2` דוחס שתי ברכות לעמוד אחד, `photoLayout: 'smart'`
ממזג שתי תמונות פורטרט, ו-`padToSpread` מכניס דפים ריקים שתופסים מספר
ולא שייכים לאף אחד. כרטיסייה 14 היא בקביעות עמוד 19.

**נגזר, לא נשמר.** כל גרירה וכל פיצול מזרימים מחדש את כל הספר. מספר
שמור היה הופך לשקר בטוח לכל ברכה שאחרי העריכה, תוך שנייה.

**איזו מוסכמת מספור.** `expandBookPages` מחזיר מערך מאונדקס-0 בלי
מספרים; המיקום *הוא* העמוד. מה שהופך את זה למספר לבן אדם היא מוסכמה,
ובקוד יש היום **שתיים**:

- `picabook-export` (הספר הריבועי 20×20 — המוצר בפועל) רץ על הברכות
  בסדר האדמין, עם `padToSpread: true, spreadOffset: 1`, ומקבץ קבצים
  `index + 1`. **זו המוסכמה שבחרתי**, ו-`PRINT_LAYOUT` בקובץ הוא בדיוק
  אותם דגלים.
- מסלול ה-PDF של Lulu ב-`/viewer` עושה `entries.reverse()` לפני
  ההרחבה, ואז מרנדר לפי סדר המערך. כלומר עמוד 1 שלו הוא **הברכה
  האחרונה** בסדר האדמין — הפוך מ-Picabook.

⚠️ **זה אומר ששני מסלולי ההדפסה מייצרים את הספר בסדר הפוך זה מזה.**
לא נגעתי בזה כי זו שאלה על המוצר ולא באג של מודול אחד, אבל צריך
להכריע: או ש-Lulu מדפיס הפוך, או שהוא בכוונה מוגש הפוך בגלל כריכה RTL.
עד שזה יוכרע, התגית אומרת את המספר של Picabook.

יש טסט שקורא את הקוד של `picabook-export` ומוודא שהוא עדיין קורא ל-
`expandBookPages` עם אותם דגלים. הערה בסגנון "שמרו על סנכרון" היא לא
מנגנון; זה כן.

**מוצג לסופר-אדמין בלבד** (`adminUser`). המספרים זזים בכל סידור מחדש,
ומספר שרץ מול הזוג מזמין "תעבירו את אמא לעמוד 5" לספר שעוד לא עוצב.

## דריסות עיצוב פר-עמוד

`src/lib/pageStyle.js` + `entry.pageStyle` ב-Firestore. עמוד בודד יכול
לחלוק על עיצוב הספר: ריווח תמונה, גודל, חיתוך, מסגרת, רקע, ריפוד,
טיפוגרפיה.

**הדריסה דלילה.** היא מחזיקה רק את המפתחות שנעלו במפורש, וכל השאר ממשיך
לרשת. זה מה שהופך את זה לבטוח: החלפת פריסט עדיין מעצבת מחדש כל עמוד,
כולל עמודים עם דריסות, בכל ציר שלא ננעל ידנית. בלי זה, "תיקנתי עמוד
אחד" היה הופך ל"העמוד הזה קפא ב-2026".

**מה אסור לדרוס:** `autoSplit`, `splitThreshold`, `entriesPerPage`,
`photoLayout`. הם מחליטים אילו עמודים *קיימים*, ונצרכים על ידי
`expandBookPages` הרבה לפני שנבנה אובייקט עמוד. דריסה שלהם היא מעגלית —
העמוד שמבקש להתחלק אחרת לא קיים עד שהחלוקה כבר רצה. חסומים ברשימה
לבנה, לא בתיעוד, ויש טסט שנכשל אם מישהו יוסיף אותם.

**נקודת המיזוג אחת:** `BookPageTemplate` בשורה הראשונה. כל 12 מקומות
הרינדור — הפליפבוק, המהדורה הדיגיטלית, שלושת ייצואי ה-PDF, ייצוא
Picabook, התצוגה בדף התודה — עוברים דרך הקומפוננטה הזאת. מיזוג אחד שם
הוא ההבדל בין שהעבודה של Lord תופיע בכל מקום לבין שתופיע במסכים שמישהו
זכר לתקן.

הסדר קריטי: המיזוג רץ **אחרי** הרזולוציה של הקורא (כולל
`withNoCropOverride`) ו**לפני** `resolvePhotoStyle`. זה מה שמאפשר לעמוד
אחד לחתוך בתוך ספר במצב אלבום — ההוראה הספציפית יותר מנצחת.

**הכתיבה דרך שרת** (`/api/entries/page-style`), כי
`firestore.rules` אוסר `update` על entries. שים לב: יש בקוד קריאות
`updateDoc` מהלקוח ל-entries (מיסגור תמונה, `forceSplit`) שאמורות
להיחסם לפי הקובץ הזה. או שהחוקים בפרודקשן שונים מהריפו, או שהן נכשלות
בשקט. **שווה בדיקה.**

**ה-UI** ב-`/wedding/[weddingId]/viewer` מתחת לרייל העיצוב הגלובלי,
סופר-אדמין בלבד. כל פקד מתחיל *בירושה* ומציג את הערך של הספר; ברגע
שמזיזים אותו הוא נעול, אומר את זה, ויש דרך חזרה — פר-פקד ולגמרי. תצוגה
מקדימה חיה מרנדרת את ה-`BookPageTemplate` האמיתי עם הסגנון הממוזג
האמיתי, כי כל דבר פחות מזה זה לעצב מול קירוב ולגלות אצל המדפיס.

`bookPages.js` בונה את עמוד התמונה של פיצול שדה-שדה, אז `pageStyle`
נוסף שם במפורש. בלי השורה הזאת פיצול עמוד היה מוחק בשקט את העבודה
בדיוק על החצי שכיווננו.

## ביקורת השיחות האמיתיות (11.8) ומה תוקן בעקבותיה

נקראו: מאיה (נסגרה על מודפס), חני (מוכן לשלם), Hasid (נפסל בצדק —
אירוע בלי סמארטפונים), שירלי (מהיום). הכתיבה עצמה טובה — מחיר ניתן
מיד כששאלו, עברית טבעית, פסילה מכובדת. שלוש תקלות התנהגות חזרו:

1. **הצעה להראות בלי לשלוח.** שירלי ביקשה את הסרטון מהמודעה; הבוט ענה
   "יש לי תמונות של ספרים אמיתיים, אשמח להראות לך" — עם image: null.
   72 קריאות מודל, אפס תמונות. זו אותה פתולוגיה של התחמקות מהמחיר:
   המודל מספר על היכולת במקום להשתמש בה. תוקן ב-`mediaGuard.js` —
   רשת דטרמיניסטית: לקוח ביקש לראות או שהבוט הבטיח להראות ⇒ מוצמדת
   התמונה הנכונה לפי סוג האירוע והשאלה. וידאו לעולם לא נבחר אוטומטית.
2. **חזרתיות.** חני קיבל את קישור התשלום שלוש פעמים, ו"איך קוראים לבן
   המצווה?" פעמיים בלי מענה. נוספו כללי אל-תחזור ל-CONVERSATION_CRAFT.
3. **באג הפרסר** (תוקן בקומיט הקודם): מפתח מדיה שהועלה נפסל מול ששת
   הקבועים.

מודול התמונה ב-Make קיים (מודול 10, פילטר "הסוכן ביקש תמונה") — הצנרת
לא הופעלה מעולם כי המודל לא בחר. עם ה-guard זה אמור להישלח בפועל;
לוודא בשיחה הבאה.

### פולו-אפים — גילוי חשוב

ה-cron של Vercel קרא לראוט שמרכיב הודעות, **מסמן אותן כנשלחו, ומחזיר
JSON לקורא שאין לו ידיים**. Vercel cron לא שולח וואטסאפ. אילו
CRON_SECRET היה מוגדר, כל בוקר היו "נשלחים" עד 25 פולו-אפים לשום מקום.
זה שהיו 26 ממתינים זה מה שחשף את זה — ה-cron קיבל 401 כי הסוד לא הוגדר.

תוקן: הראוט מזהה מי הקורא. Make (סוד משותף) — כמו קודם, הקורא שולח.
אחרת, אם מוגדרים `WHATSAPP_TOKEN` + `WHATSAPP_PHONE_ID` — הראוט שולח
בעצמו (`whatsapp.js`, Graph API): טקסט + תמונה בתוך חלון 24ש; מחוץ
לחלון השליחה נכשלת, הליד נשאר due, והשגיאה על ה-item. בלי אף אחד
מהשניים — מרכיב בלבד, לא מסמן כלום, ומחזיר `delivery: 'none'` עם
אזהרה. שקר על משלוח הוא הדבר היחיד שאסור.

**מה Lord צריך להגדיר ב-Vercel:** `CRON_SECRET` (כל מחרוזת),
`WHATSAPP_TOKEN` (טוקן קבוע של system user מ-Meta Business),
`WHATSAPP_PHONE_ID` (המזהה של 972515350253). ואז ה-cron של 10:30
באמת שולח.

**הסרטון מהמודעה:** לקוחות מבקשים אותו בשם. להעלות אותו למסך המדיה
(עד 16MB MP4) + מודול וידאו ב-Make, ואז הבוט יוכל לענות עליו במקום
להתנצל.

## סטטוס תפעולי (10.8) — וואטסאפ, סביבה, והתקרית

**סביבה ב-Vercel:** `CRON_SECRET` (הערך שהוגדר שונה מהמחרוזת שנשלחה
בצ'אט — לא קריטי, ה-cron של Vercel מזריק אותו בעצמו), `WHATSAPP_PHONE_ID=1158941773964795`,
`WHATSAPP_TOKEN` (system user "WeddingTales", אפליקציית **WT Sales
Messaging** שנוצרה היום עם use case של WhatsApp, טוקן ללא תפוגה,
הרשאות messaging+management, ה-WABA "בר מספר הברכות" משויך). אומת:
`whatsappConfigured: true`.

**מגבלת חשבון ב-Meta מאז 24.5:** "החשבון הושבת כי האתר הרשום לא
נמצא" — למרות שהאתר חי. עודכן פרופיל העסק (אתר:
app.weddingtales.co.il/lp, כתובת מלאה) והוגש ערעור. עד להסרה: אין
פתיחת שיחות יזומות. שיחות נכנסות דרך Make ממשיכות לעבוד בפועל.

**תקרית ה-dry run (מתועדת גם בקומיט f508170):** ההרצה רצה על הקוד
שלפני התיקון ושלחה בפועל דרך ה-API. Meta החזירה 200 על כל 25 (ה-API
אסינכרוני — קבלה ≠ מסירה). 24 מתוך 25 היו מחוץ לחלון 24ש ולכן ייכשלו
במסירה ולא יגיעו לאף לקוח. אחת — Kateryna Curiel — הייתה בתוך החלון
וכנראה קיבלה את הפולו-אפ ("היי, רק רציתי לבדוק אם הספקת לראות את
הדמו..."). טקסט לגיטימי; הסיכון היחיד: היא לא סומנה (בגלל שאר שערי
ה-dry) ולכן ה-followUpAt שלה הוזז ידנית ל-12.8 כדי למנוע כפילות מחר.
ההודעה גם לא כתובה ב-turns שלה — אם תענה, הבוט לא יזכור שהוא שלח.

**ריצה מלאה של 25 לידים ארכה 28.5 דקות** על הקוד ללא timeout — כל
שליחה מחוץ לחלון כנראה נתלתה דקות. עם ה-timeout של 15ש בקוד הנוכחי
הריצה חסומה ב~2-3 דקות. `maxDuration: 60` בראוט לא נאכף בפועל (Fluid).

**מה נשאר לבוט:**
- ברגע שהמגבלה תוסר: להריץ פולו-אפים (26 ממתינים, dry קודם), ולבדוק
  שליחת תמונה בשיחה חיה (mediaGuard פרוס אך טרם נצפה בפעולה).
- תבנית `wt_followup` (MARKETING) — תנאי לפולו-אפים מחוץ לחלון 24ש.
  חסום עד הסרת המגבלה.
- סרטון המודעה מפייסבוק: להעלות למסך המדיה + מודול וידאו ב-Make.
- טסט תגובות אינסטגרם (Advanced Access) — פתוח מהשבוע שעבר.


## פתיחות מגוונות (סבב משוב שני של Lord)

הזרועות ב-`experiments.js` הוחלפו בשבע גישות שונות באמת, לפי הבקשה
המפורשת שלו: קצר-ושאלה, דמו מיד, **דוגמה אישית מתמונה** ("שלחי תמונה
ואכין דוגמה"), **הצעת שיחה** ("מתי נוח שנדבר?"), **העוזר האישי**,
**תמונות קודם** (תמונה כבר בהודעה הראשונה — ההנחיה גוברת על האיסור
הכללי), ומחיר מראש. `value_then_ask` הוסרה. שבע זרועות לומדות לאט
מארבע — הוא בחר רוחב על פני מהירות במודע.

שתי זרועות מסתיימות ב-handoff בכוונה (דוגמה מתמונה, קביעת שיחה):
את הדוגמה ואת השיחה עושה בן אדם. תפקיד הבוט להשיג את הכן ולהעביר.

⚠️ פער ידוע: הודעת מדיה נכנסת (תמונה מהלקוח) מגיעה מ-Make בלי טקסט
והראוט שותק עליה. בזרוע photo_sample זה אומר שלקוח ששלח תמונה עלול
לקבל שקט. עד שנחווט זיהוי מדיה נכנסת ב-Make (למפות את סוג ההודעה
ולא רק טקסט), ה-handoff קורה כשהם *מסכימים* בטקסט; אם שלחו תמונה
ישר — זה ייתפס בסריקה של הליד או בעין של Lord בפאנל. לתקן בהמשך:
מודול Make שמעביר גם type + media id.


## החלפת תמונה בעמוד בודד

`src/lib/entryPhoto.js` + `imageUrlOverride` / `imgAspectOverride` על
מסמך ה-entry. בעורך העמוד ב-viewer יש עכשיו שדה "התמונה בעמוד":
תמונה ממוזערת, בורר קובץ להחלפה, ו"חזרה לספר" שמשחזר.

**התמונה של האורח לא נדרסת לעולם.** ההחלפה יושבת בשדה נפרד ומנצחת
בקריאה; שחזור הוא החזרת שדה ל-null, לא שחזור נתונים. זו התרומה של
מישהו אחר למזכרת, ומוצר שמוחק אותה בשקט לקח החלטה שאין לו זכות לקחת.

**הרזולוציה ב-`getEntries()` ולא ברינדור.** כל המשטחים טוענים ברכות
משם — הפליפבוק, המהדורה הדיגיטלית, שלושת מסלולי ה-PDF, ייצוא Picabook,
ניהול הברכות, וגם `expandBookPages` שקורא `imageUrl` כדי להחליט אם
עמוד מתפצל לשניים. רזולוציה בתבנית הייתה מכסה רינדורים ומפספסת חלוקה
לעמודים.

`imgAspect` נזרק כשמחליפים (אלא אם נמדד בדפדפן ונשמר): ירושה של יחס
הממדים של התמונה הישנה הייתה ממסגרת את החדשה לקופסה שהיא לא מתאימה
לה — בדיוק הבאג ש"ללא חיתוך" קיים כדי למנוע.

הקובץ עולה מהדפדפן ישירות ל-Storage תחת `weddings/{id}/replace_*`
(כלול בכללי האחסון הקיימים: תמונה עד 8MB), ורק ה-URL נשלח ל-API.

מיסגור (נקודת מיקוד/סיבוב) נשמר מהתמונה הקודמת — אם הוא לא מתאים
לחדשה, מתקנים במסך ניהול הברכות.


## טבלת האירועים בסופר-אדמין — תוויות, חיפוש, סינון ומיון

`src/lib/adminEventsView.js` (טהור, נבדק). שלוש תלונות, סיבת שורש אחת לכל:

**התוויות שיקרו.** `EventTypeBadge` היה קיים, אבל `normalizeEventType`
מחזיר `'wedding'` גם ל**חסר** — אז כל אירוע שלא סווג ענד תווית "חתונה"
בטוחה ושגויה. גרוע מאין תווית, כי אי אפשר להבדיל בין חתונות אמיתיות
לבין רשומות שלא סווגו. `eventTypeOf` מחזיר `null` ולא מנחש, והתווית
אומרת "לא הוגדר".

**החיפוש פספס.** חיפש רק couple label, אימייל, מזהה הזמנה ומזהה מסמך.
לא את שם החוגג — שבבר מצווה זה *שם האירוע* והדבר שזוכרים — לא את שדות
השם בעברית, ולא את הטלפון. עכשיו: כל שדה שבן אדם זוכר, טוקנים ב-AND
(עוד מילה מצמצמת, לא מרחיבה), וטלפון מושווה בספרות בלבד כך ש-"052-661"
מוצא את "0526618184". טוקן של פחות מ-3 ספרות לא מושווה לטלפונים —
"52" קיים בכל מספר והיה הופך את החיפוש לחסר משמעות.

**מיון לפי סכום.** `compareByAmount` מוריד את מי שלא שילם לתחתית
**בשני הכיוונים**. האסימטריה מכוונת: החץ מסדר תשלומים אמיתיים ולא
מקדם אפסים למסך הראשון. "מי לא שילם" זו שאלה אחרת, ויש לה צ'יפ משלה.

נוסף גם סינון לפי סוג אירוע (עם מונים בצ'יפים, כולל "לא הוגדר" כרשימת
מטלות), עצמאי מסינון הסטטוס — "בר מצוות" ו"לא שולם" הן שאלות שונות
והשילוב ביניהן הוא המקרה השימושי.


## צביעת שורות לפי דחיפות בטבלת הסופר-אדמין

`rowUrgency` / `daysUntilEvent` / `countdownLabel` ב-`src/lib/adminEventsView.js`
(טהורים, נבדקים). שני חצאים של אותה שאלה — במה צריך לטפל עכשיו:

* **`soon`** — אירוע בתוך `SOON_WINDOW_DAYS` (14) ימים כולל היום. ענבר.
  עבר → `null` בכוונה: אירוע שהיה כבר לא דורש כלום, וצביעה שלו הייתה
  מבזבזת את הסיגנל החזק ביותר בטבלה על השורות הרגועות.
* **`nodate`** — ורוד. חסר תאריך זה גרוע מדחוף, זה **בלתי נראה**: כל
  דדליין במערכת מחושב מ-`weddingDate`, אז שורה בלי תאריך יוצאת בשקט
  מתווית הסטטוס, מסינון "קרובות", ממיון התאריך וגם מסולם המעקב. היא לא
  נראית מאחרת. היא נראית בסדר.

**מחלקות ולא `style` inline.** רקע ב-`style` מנצח כל כלל `hover` בגיליון,
כך ששורה צבועה הייתה מתה תחת הסמן — דווקא השורה שאליה מושיטים יד.

**ספירה לאחור בתא התאריך** ("היום" / "מחר" / "בעוד N ימים"). בלעדיה כל
ארבע-עשרה השורות הענבריות נראות דחופות באותה מידה; הצבע אומר "אלה",
הספירה אומרת "זו קודם".

`eventDateMs` מקבל את הצורות שהשדה באמת מגיע בהן (ISO, Date, מילישניות,
Timestamp עם `toDate()` או אחרי JSON עם `seconds`), ומחזיר `null` ולא
ניחוש — צבע שגוי גרוע מאין צבע, כי סומכים עליו. הנרמול זהה ל-
`getWeddingStatus()` בכוונה: שורה צבועה "קרוב" לצד תווית שאומרת אחרת
הייתה באג גרוע משתי ההתנהגויות בנפרד.

## גודל פונט ורוחב ברכה ברמת העמוד

נוספו ל-`PageStyleControls` שני סליידרים: `fontSizePercent` (1.5–6%) ו-
`textMaxWidth` (40–100%). שני המפתחות כבר היו ב-`PAGE_OVERRIDABLE_KEYS`.

**הבעיה האמיתית הייתה שהסליידר משקר.** הטמפלייט מקטין טקסט ארוך
(`fontFitFactor`) והמכפיל חל על כל מה שהסליידר אומר: לגרור עמוד ארוך
ל-5% מרנדר 3.1%, הסליידר ממשיך להראות 5, ושום דבר במסך לא מסביר למה
הטקסט לא זז. לכן:

* הנוסחה עברה ל-`src/lib/fontFit.js` (`pageFitFactor` / `duoFitFactor`,
  נבדקים) והייתה משוכפלת ב-`BookPageTemplate` וב-`DuoPageLayout`. עכשיו
  יש מקור אחד, והעורך מייבא אותו במקום להעתיק — שלושה עותקים של נוסחה
  זה שלוש הזדמנויות לסטות, וסטייה כאן לא נראית עד שהספר חוזר מהדפוס.
* כשההקטנה פעילה, ה-hint מציג את הגודל **בפועל**.
* ומופיע מתג "להשאיר בגודל" שמציב `fontMinFactor: 1` — הרצפה מורמת עד
  התקרה, כלומר ביטול ההקטנה לעמוד הזה בלבד. מופיע רק כשההקטנה באמת
  קורית: מתג למשהו שלא מתרחש הוא רעש, ובטקסט ארוך מאוד הוא עלול לחרוג
  מהעמוד — לכן הוא צריך להופיע בדיוק במקום שבו הוא התשובה לבעיה גלויה.

`DuoPageLayout` ממשיך להתעלם מ-`fontFitTarget` (מספר שמכוון לעמוד שלם
היה משאיר חצי עמוד כמעט בלי הקטנה) — רק הרצפה משותפת. זו התנהגות
קיימת שנשמרה במכוון, ויש עליה בדיקה.


## שם האורח ברמת העמוד

נוספו ל-`PageStyleControls`, בראש הרשימה: גודל שם האורח, יישור (כולל
"לפי השפה"), וריווח מלמעלה/מלמטה. הקבוצה ראשונה כי השם הוא הדבר העליון
בעמוד — הפאנל נקרא באותו סדר שהעמוד נקרא.

**לשם אין גודל משלו כברירת מחדל** — הוא עוקב אחרי הברכה ב-70%
(`nameFontPercent` ב-`src/lib/fontFit.js`). הנוסחה הייתה כתובה בארבעה
מקומות: שני הטמפלייטים, `StartWizard`, וסליידר הסטודיו — שם היא אפילו
לא הייתה נכונה (`?? 2.1` קבוע, שמשקר ברגע שגודל הברכה שונה מ-3).
הסליידר בעורך מציג את הערך **הנגזר**, כך שהוא נשאר אמיתי אחרי שמזיזים
את גודל הברכה שמעליו.

`!= null` ולא בדיקת truthiness, בהתאם ל-`??` שהיה בטמפלייטים: 0 שמור
הוא בחירה מכוונת, `null` זה "לא הוגדר, עקוב אחרי הברכה".

**מתי הפקדים לא עושים כלום, והפאנל אומר את זה.** הטמפלייט מרנדר את בלוק
השם כשיש שם, וגם כשיש תמונה ואין שם — רפאים בלתי נראים שמחזיקים את
השורה והריווחים כדי שהתמונה תנחת באותו גובה כמו בעמוד חתום. בעמוד בלי
שם ובלי תמונה הבלוק לא מרונדר בכלל, ואז ארבעת הסליידרים לא משנים כלום.
`nameState` מזהה את שני המצבים ומסביר במקום להשאיר פקדים שנראים פעילים.


## שמירה בעורך העמוד — שני באגים שתוקנו

**1. מתג ההקטנה האוטומטית לא שמר כלום בצד אחד.** צד אחד עשה `set`
והשני `unpin`. "להקטין כדי להיכנס" תורגם ל"תירש מהספר", כלומר **לא
נשמר שום דבר** — ובספר שהעיצוב הגלובלי שלו אומר לא להקטין, הלחיצה לא
עשתה כלום ולא שרדה רענון. שני הצדדים נועצים עכשיו ערך מפורש
(`DEFAULT_MIN_FACTOR` / `1`), וההסרה עוברת דרך ״חזרה לספר״ כמו בכל
שאר הפקדים. **הכלל: פקד נועץ בשינוי, ומתנתק רק דרך הכפתור הייעודי.**

**2. שינוי ב-450 מילישניות האחרונות לפני יציאה מהעמוד נזרק.** ה-debounce
לא נשלח ב״סיום״ ולא במעבר לעמוד אחר, ובניקוי ה-unmount הוא **בוטל**
במפורש (`clearTimeout`) — כלומר סגירת הפאנל זרקה פעילות את השינוי
האחרון. עכשיו יש `flushNow()` וכל יציאה עוברת דרכו: סיום, מעבר עמוד,
unmount.

בנוסף, `entryId` נוסע עכשיו **עם המטען** ולא נקרא מה-state בזמן
השליחה — כתיבה שעוד על ה-debounce כשעוברים עמוד חייבת לנחות על העמוד
שנערך, לא על זה שמסתכלים עליו עכשיו. זה היה באג ממתין להתרחש.


## עיצוב אורחים שלישי — "ערב"

`designVariant: 'night'`. לוח זכוכית מואר על מרפסת ירושלמית בשעת ערב,
כרטיס נייר שמנת, וזהב. נבנה כ**וריאנט** ליד poker ו-romantic — אף אירוע
קיים לא משתנה.

**המסגרת היא התמונה, לא CSS.** הלוח, האורות והאבן אפויים ב-
`/backgrounds/nightglass.webp`, והטופס יושב בתוכם. ניסיתי קודם לצייר
לוח זכוכית ב-CSS מעל רקע מטושטש — זה נכשל: טשטוש הורס את האורות ובכל
זאת לא מעלים את קווי הלוח האפויים, כך שמתקבלות **שתי מסגרות** שנלחמות
זו בזו. עמודת התוכן היא 79% מרוחב המסך (מקסימום 400px) עם ריפוד עליון
של ~16.5vh, מה שמצליב אותה עם הלוח בכל מסך טלפון — כי `cover` על מסך
צר-וגבוה חותך למעלה/למטה ומשמר את היישור האופקי.

**ארבעה מפתחות חדשים, כולם עם נפילה לאחור.** `photoCardBg`,
`photoCardBorder`, `photoWellBg`, `photoWellBorder`. עד עכשיו שני
הכרטיסים קראו `cardBg`; כאן הם חייבים להיות שונים — הראשון הוא נייר
שכותבים עליו, השני חייב להישאר **חלון**. משטח שמנת במקום הנוף היה חוסם
את החלק היחיד מהתפאורה שהאורח עוד רואה, והמסגרת המקווקוות בזהב היא מה
שאומר "גררו לכאן תמונה" ולא "עוד טופס". כל וריאנט אחר נופל חזרה לערך
הישן ולא זז בפיקסל.

`isNight` לא מותנה ב-`eventType` (בניגוד ל-`isRomantic`). הבורר
בסופר-אדמין נפתח עכשיו לכל סוגי האירועים — הוא היה מוסתר מאחורי
`eventType === 'wedding'`, כלומר היה מסתיר את העיצוב הזה בדיוק מבר
המצווה שבשבילו הוא נבנה. 'romantic' נשאר לחתונות בלבד: הקופי והנכס
הפרחוני שלו ייעודיים.

**זרימת התמונה נשמרה כפי שהיא** — באר 4:3, דיסקית מצלמה ושני הכפתורים
(צילום / גלריה). המוקאפ הראה אזור גרירה פשוט; להחליף אליו היה מוחק את
אפשרות הצילום הישיר, שהיא הדרך שרוב האורחים באירוע מוסיפים תמונה.


### מספרי הפריסה של "ערב", ומאיפה הם

לא ניחושים — רונדרתי את הטופס האמיתי מעל הנכס האמיתי בגדלים אמיתיים
(390×844, 360×800) וצילמתי, עד שזה ישב נכון:

* `paddingTop: 15vh` — הפס העליון של הלוח נמצא ב-13.5% מגובה הנכס.
  ה-`pt-8` המקורי היה משאיר את הכותרת **מעל** המסגרת, מרחפת בין
  שרשרות האורות.
* `maxWidth: min(84vw, 380px)` — הלוח הוא 77.6% מרוחב הפריים.
  עמודה של 26rem גולשת מעבר לפסים הצדדיים.
* כותרת 22px במקום 26px — ב-26 היא נשברת לשתי שורות בעמודה הצרה
  יותר, והשורה השנייה נוחתת בדיוק על הפס העליון.
* תת-כותרת מוסתרת, ותיבת הברכה 112px במקום 128 — הלוח בגובה קבוע, וכל
  שורה שמבזבזים למעלה דוחפת את כפתור השליחה אל מתחת לקיפול.

**היישור האופקי יציב בכל טלפון** כי `cover` על מסך צר-וגבוה מקטין לפי
הגובה וחותך למעלה/למטה — לא בצדדים. לכן היחס בין רוחב העמודה לרוחב
הלוח נשמר, וזה מה שמחזיק את הפריסה.


## עיצוב אורחים רביעי — "בוקר", והפריסה שעברה ל-theme

`designVariant: 'dawn'`. אותו לוח זכוכית, הפעם מול הכותל באור בוקר.
**היפוך מלא של "ערב", לא ואריאציה:** על אבן ירושלמית בהירה זהב נעלם
וכרטיס שמנת על קיר שמנת הוא לא כרטיס. לכן ברונזה עמוקה על לבן-חמים,
וכפתור **כהה** עם טקסט לבן — הכפתור הבהיר של "ערב" היה נבלע כאן, וזה
הפקד היחיד שאסור שיהיה קשה למצוא.

### מספרי הפריסה עברו ל-theme

`patch7` כתב את הפריסה של "ערב" לתוך הטופס כ-`isNight ? A : B` בחמישה
מקומות. הרקע השני הוא **צילום של מסגרת אחרת**: הפס העליון שלו ב-10.2%
מגובה הפריים במקום 13.5%, והלוח ברוחב 82.8% במקום 77.6%. חמישה
טרנארים היו הופכים לחמש שרשראות תלת-כיווניות, ואז ארבע.

עכשיו הטופס קורא `formPaddingTop`, `formMaxWidth`, `titleFontSize`,
`titleShadow`, `hideSubtitle`, `textareaHeight` — **ולא יודע איזה
וריאנט פעיל**. כולם עם נפילה לאחור, אז מי שלא מגדיר אותם לא זז.
`titleShadow` הוא ערך ולא דגל דווקא בגלל הזוג הזה: תפאורה כהה רוצה צל
כהה, בהירה רוצה הילה לבנה.

המספרים נמדדו מהנכסים עצמם (סריקת מפלי בהירות לאיתור הפסים), ואומתו
ברינדור של הטופס האמיתי מעל הנכס ב-390×844 וב-360×800.


### "ברכה ותמונה בלבד", ולא לחרוג מהלוח

בשני העיצובים הממוסגרים ירדו **שדה השם**, הקישוט שהפריד בינו לברכה,
ושורת האמון — `hideNameField` / `hideTrust`. זה בערך 145px, וזה מה
שמכניס את הטופס לתוך הלוח במקום שיגלוש מתחת לפס התחתון אל הבסיס.

**השם מוסתר, לא בוטל.** `name` עדיין מחזיק את מה שקישור מותאם
(`?g=`) סיפק, וזה עדיין מגיע לספר. בלי קישור כזה הברכה מגיעה בלי שם —
`BookPageTemplate` כבר יודע לטפל בזה (רפאים בלתי נראים שמחזיקים את
הגובה). **זו התנהגות מכוונת של העיצובים האלה, לא באג.**

**הפסים נמדדו מהנכסים, לא נוחשו:**

| | שמאל | ימין | עליון | תחתון |
|---|---|---|---|---|
| night | 10.5% | 89.6% | 14.4% | ~86.5% |
| dawn | 8.6% | 91.4% | 10.2% | ~89% |

**רוחב העמודה מכיל איבר ב-vh, וזה העיקר.** עם `cover` על מסך גבוה-וצר
ההקטנה נקבעת לפי **הגובה**, ולכן רוחב הלוח על המסך הוא שבר מגובה
החלון ולא מרוחבו: 0.791 × 0.563 = 44.5vh אצל night. עמודה ב-vw בלבד
הייתה נסחפת מול הפסים בכל יחס מסך אחר. אומת ב-390×844 וב-360×800 מול
הפסים המדודים — אפס חריגה בשניהם.

**`MySubmissions` עבר ל-`order-last`** בעיצובים הממוסגרים. לאורח חוזר
הוא רץ מעל הכותרת והיה דוחף את כל ההרכב אל מחוץ ללוח.


### הכותרת בעיצובים הממוסגרים, והגעה עד הפס התחתון

**כותרת:** Frank Ruhl Libre במשקל 500 במקום sans מודגש, ומתחתיה קו
דק–מעוין–קו — אותה שפת קישוט שכבר קיימת בין המקטעים בכרטיסים. `titleFont`
הוא **שם** ב-theme, והמיפוי לפונט האמיתי יושב ב-`photo/page.js`:
`guestPageTheme.js` חייב להישאר מודול נתונים טהור שאפשר לייבא ב-vitest,
ו-`next/font/google` בתוכו היה מפיל את כל חבילת הבדיקות באיסוף.

**הבאר הריקה של התמונה ירדה מ-4:3 ל-3:2** בעיצובים הממוסגרים. 4:3 הוא
היחס של *תמונה*, וזה עדיין מה שהיא הופכת להיות ברגע שיש אחת; ריקה היא
מחזיקה דיסקית ושני כפתורים בהרבה אוויר — ובתוך לוח בגובה קבוע האוויר
הזה הוא הגובה של תיבת הברכה. שני ה-literals נשארים ב-JSX כדי ש-Tailwind
יראה אותם.

**גובה תיבת הברכה שונה בין השניים, וזה נכון:** night 96px, dawn 128px.
נמדד — ב-night, 96px מותיר 7px מעל הפס התחתון ב-360×800 ו-112px כבר
חוצה אותו. הלוח של dawn גבוה יותר וקונה 32px נוספים.

**הערה למדידה עתידית:** בין שני הכרטיסים יש מפריד מקטע
(`mb-4 mt-2` + תווית) ששווה ~44px. פרוטוטייפ שמשמיט אותו נותן מספרים
אופטימיים ב-30px ומעלה — זה בדיוק מה שקרה בסבב הראשון כאן.


### תצוגה מקדימה של עיצוב אורחים: `?dv=`

`designVariant` נקרא ממסמך האירוע ומשום מקום אחר, כך שהדרך היחידה לראות
עיצוב הייתה **לשמור אותו על אירוע אמיתי** — שהאורחים שלו סורקים את
ה-QR באותו רגע. `?dv=night` / `?dv=dawn` דורס את הערך לטעינת העמוד
הנוכחית בלבד: לא נכתב כלום, אף אחד אחר לא רואה, והסרת הפרמטר מחזירה את
העמוד למה שלאירוע באמת יש. אותו רעיון כמו `?gd=` שהסטודיו כבר משתמש בו.

**איפה בוחרים בפועל:** סופר-אדמין ← לחיצה על שורת האירוע ← מגירת
הפרטים ← `EventTypeEditor` ← "סגנון העיצוב". **לא** במסך "עיצוב דף
ברכה" שבסטודיו — המסך ההוא עורך `guestDesign` (דריסת צבעים חופשית),
לא בוחר וריאנט.


### למה בחירת "ערב"/"בוקר" לא שינתה כלום באירוע מסוים

`guestDesign` — הפריסט שנשמר במסך "עיצוב דף ברכה" בסטודיו — נפרס
**מעל** הבסיס שהוריאנט מייצר. באירוע שיש לו פריסט שמור זה אומר
ש-`pageBgImage`, `pageBg`, `cardBg` וכל הצבעים מגיעים מהפריסט, ולכן
בחירת "ערב" לא שינתה שום דבר שרואים. זה מה שקרה.

**החלה חלקית היא לא תשובה טובה יותר.** פריסט סטודיו הוא פלטה קוהרנטית
שנכתבה מול **עמוד שטוח**: ה-`titleColor` שלו חום כהה כי הרקע שנהב.
מעל צילום לילי הכותרת פשוט נעלמת. אין תת-קבוצה של המפתחות האלה ששורדת
מעבר למסגרת צילומית — אז אף אחד מהם לא עובר.

לכן: **עיצוב ממוסגר מתעלם מ-`guestDesign`.** שום דבר לא נמחק — הפריסט
נשאר על המסמך וחוזר ברגע שמחזירים את האירוע לעיצוב שטוח. יש הערה על כך
מתחת לבורר, שמופיעה רק כשבאמת בחרו ערב/בוקר.


### שני לייאאוטים בעמוד הברכה — ולמה "ערב"/"בוקר" לא הופיעו כלל

`photo/page.js` מכיל **שני** לייאאוטים:

* **moment** — `isMomentLayout = !isPoker && !isRomantic`. כלומר **כל
  אירוע רגיל**, כולל כל בר מצווה, מגיע לכאן. הוא בונה אובייקט עיצוב
  משלו (`md`) inline **ולא קורא ל-`buildGuestPageTheme` בכלל**.
* **classic** — שני כרטיסים (שם+ברכה, תמונה), מונע מ-`theme`. עד עכשיו
  רק romantic ו-poker הגיעו אליו.

כל מפתחות ה-theme של העיצובים הממוסגרים חיים ב-**classic**. לכן בחירת
"ערב" על אירוע אמיתי לא שינתה כלום — העמוד רונדר moment, שלא מסתכל על
ה-theme בכלל. זה מה שדווח.

**התיקון: עיצוב ממוסגר מנתב ל-classic** (`&& !framed`), הלייאאוט שהעיצוב
תוכנן, נמדד וצולם מולו. הפורט אל moment היה דורש מדידה מחדש של הכל מול
מבנה אחר לגמרי (פיל, כרטיס יחיד, באר גדולה).

נלווה: `hideChrome` כולל עכשיו גם framed — הכותרת והפוטר של האתר היו
יושבים מעל ומתחת ללוח הזכוכית — ו-`minHeight` עובר ל-`100vh` במקום
`calc(100vh - 4rem)`, אחרת הלוח מותאם לקונכייה שכבר לא קיימת וכל מדידות
הפסים זזות.


### "לא לצאת מהקונטיינר" — מבנית, לא בכוונון

**באר התמונה חזרה ל-4:3 בכל המצבים.** קודם עשיתי אותה 3:2 בריק ו-4:3
אחרי בחירת תמונה, כדי לקנות גובה לתיבת הברכה. זו בדיוק העסקה ההפוכה:
הכרטיס **גדל בדיוק ברגע שהאורח בוחר תמונה**, וזה הרגע שבו הוא חוצה את
הפס התחתון. חוץ מזה 4:3 הוא האמת — זה מה שהקרופר מייצר וזה מה שהספר
מדפיס, אז תצוגה מקדימה בקופסה אחרת היא תצוגה של משהו אחר.

**`formMaxHeight` — תקרה אמיתית לעמודה** בעיצובים הממוסגרים, מחושבת
מאותם פסים מדודים: night `70vh` (פסים 14.4%–86.5%, התחלה ב-16vh), dawn
`77vh`. עם `overflow-y:auto` ו-`overscroll-behavior:contain`.

**למה זה נחוץ למרות כל המדידות:** כל מספר אחר כאן כוון מול הטופס
**הריק**. שורת שגיאה, גופן מערכת גדול יותר, רצועת אורח חוזר — וזה גולש.
התקרה הופכת את "זה לא יוצא מהאקריל" מ**טענה על התוכן** ל**תכונה של
הפריסה**. במצב הרגיל שום דבר לא מגיע אליה ואין פס גלילה שרואים.

**כל תמונה נראית נכון** כי מצב התצוגה הסופית הוא `object-contain` —
שום תמונה לא נחתכת בתצוגה המקדימה, ותמונה לאורך מוצגת במלואה עם רקע
הסצנה מאחוריה.


### המספרים הסופיים, ואיך אומתו

רונדור של שני העיצובים, **גם ריק וגם אחרי בחירת תמונה**, ב-360×800,
390×844 ו-430×932, מול הפסים שנמדדו מכל נכס. **12 מקרים, אפס חריגה,
ואף מקרה שבו התקרה בכלל נכנסת לפעולה** — היא נשארת רשת ביטחון בלבד.

* **תיבת הברכה ב-vh עם רצפת פיקסלים** (`max(84px, 11.4vh)` /
  `max(104px, 15.2vh)`). קודם זה היה גובה קבוע שכוון על טלפון אחד:
  גדול מדי ב-360×800, קטן שלא לצורך ב-430×932. הלוח מתכייל לגובה
  החלון, אז גם הקופסה שבתוכו צריכה.
* **העמודה של night ירדה מ-37vh ל-35.5vh.** ב-360×800 הטופס חרג ב-9px,
  והתשעה הזולים ביותר **לא** היו בתיבת הכתיבה: באר התמונה היא 4:3
  מרוחב העמודה, אז עמודה מעט צרה יותר מקצרת את הבאר ולא נוגעת בקופסה.
  הכרטיס פשוט יושב קצת יותר פנימה מהפסים.


### העיצובים הממוסגרים במסך "עיצוב דף ברכה" שבסטודיו

המסך הזה עורך `guestDesign` — **פלטה**. העיצובים הממוסגרים מתעלמים
מפלטות במכוון, אז להוסיף אותם כפריסטי צבע היה מייצר שני צ'יפים שלא
עושים כלום. הם **סוג אחר של בחירה**: לא "העיצוב הזה עם הצבעים שלי" אלא
"העיצוב הזה".

לכן הפריסט שלהם נושא `variant` במקום `design`, והמסך מטפל בזה לכל
האורך: תצוגה מקדימה דרך `?dv=` (ולא `?gd=`, שלא היה מגיע אליהם בכלל),
״החל על האירוע״ כותב `designVariant`, והצ'יפ מסומן כפעיל.

**הפלטה השמורה לא נמחקת.** בחירת עיצוב ממוסגר היא החלטת עיצוב, לא
מחיקה: הפלטה נשארת על המסמך, מתעלמים ממנה, והיא חוזרת ברגע שחוזרים
לעיצוב צבעוני. יש הודעה במסך שאומרת בדיוק את זה.


## מסגרות תמונה מהעלאה — nine-slice

**מה היה:** התמונה שהועלתה נמתחה על כל תא התמונה ב-`object-fit: fill`.
זה כפה שני דברים: **שקיפות באמצע** (אחרת היא מכסה את התמונה),
ו**פרופורציות מדויקות** של התא (אחרת היא נמעכת). מכאן "PNG שקוף
ברזולוציה מסוימת" — דרישה שאי אפשר לספק ממסגרת מהמדף.

**מה יש:** `src/lib/nineSlice.js` (טהור, נבדק) חותך את המקור לרשת 3×3
— ארבע פינות, ארבעה צדדים, ואמצע ש**לא מצויר בכלל**. הפינות שומרות על
צורתן בכל גודל, הצדדים נמתחים כל אחד בציר שלו, והאמצע — החלק שהיה
צריך להיות שקוף — לא קיים. **כל רזולוציה, כל יחס, כל JPEG אטום.**

**למה לא `border-image`.** CSS עושה בדיוק את זה בשורה אחת. אבל הספר
מיוצא ל-PDF דרך `html2canvas`, ש**לא מממש `border-image`** — המסגרת
הייתה נראית מושלם בכל מסך ונעלמת מהספר המודפס, שזה המקום היחיד שבו
היא באמת חשובה. לכן `NineSliceFrame` מצייר שמונה `<img>` בתוך קופסאות
`overflow:hidden`; את זה html2canvas מרנדר נאמנה.

**כל החישוב יחסי** — מידות הפיקסלים של הקובץ שהועלה לא נכנסות למתמטיקה
בכלל. זה מה שהופך את "כל רזולוציה" למשפט נכון ולא לשאיפה.

**תאימות לאחור מלאה:** מסגרת שנשמרה לפני זה אין לה `photoFrameSlice`,
`normalizeSlice` מחזיר `null`, ו-`FramedPhoto` הולך במסלול הישן בדיוק
כמו קודם. המסגרות ההן **צוירו** למסלול ההוא. רק העלאות חדשות מקבלות
את המנגנון החדש, עם סליידר אחד: כמה אחוז מהתמונה שהעלית היא המסגרת —
המספר היחיד ש-nine-slice לא יכול להסיק לבד.


### `panelRect().visible` — ולמה הבדיקה האוטומטית פספסה לנדסקייפ

הבדיקה שכתבתי אישרה שהטופס **בתוך הפסים**. בלנדסקייפ (844×390) `cover`
דוחף את רוב הלוח אל מחוץ למסך — הפסים נוחתים על -338 ו-743 — כך
שהטופס ישב מושלם בתוך פסים ש**אף אחד לא רואה**, ונחתך על ידי החלון.
הבדיקה עברה, העין לא.

`panelRect` מחזיר עכשיו גם את **החיתוך עם החלון**, וכל מה שאחריו —
איפה למרכז, כמה להקטין — משתמש בו. בטלפון שני המלבנים זהים ושום דבר
לא משתנה; בלנדסקייפ הטופס יורד ל-0.58 ונכנס במלואו.

**הלקח:** "בתוך המסגרת" ו"על המסך" הם שני תנאים שונים. בדיקה שמאמתת
רק את הראשון עוברת בדיוק במקרה שבו השני נכשל.


### "עמוד אחד לא מקבל את המסגרת החדשה"

**אין באג ברינדור.** מסלול המסגרות ב-`FramedPhoto` אחיד לכל העמודים.
עמוד שלא עוקב אחרי עיצוב הספר הוא עמוד ש**ננעל** בעורך העמוד: מפתח
נעוץ מפסיק לעקוב אחרי הספר — זו כל המהות של הפיצ'ר.

זו גם תקלת הכשל הגרועה ביותר שלו: עמוד שמתעלם בשקט מעיצוב הספר, בלי
שום דרך למצוא אותו חוץ מלפתוח עמודים אחד-אחד. הפאנל **ידע** כמה עמודים
נעוצים ופשוט לא אמר **אילו**.

עכשיו הוא מפרט אותם — מספר עמוד ושם האורח, כל אחד כפתור שפותח את
העורך של אותו עמוד, שם הצ'יפ "נעול" ו"חזרה לספר" כבר מחכים.

**התיקון למשתמש:** viewer ← עיצוב הספר ← ברשימה למטה לוחצים על העמוד
← בשורת "מסגרת לתמונה" יופיע "נעול" ← "חזרה לספר". ("אפס" מנקה את כל
ההגדרות של אותו עמוד.)


## הדפסה: נשארים ב-Picabook (החלטה, אוגוסט 2026)

**Lulu מחוברת במלואה ולא בשימוש** — `/api/lulu/create-order` עושה auth,
חישוב עלות, יצירת print-job ומעקב סטטוס, ויש UI בסופר-אדמין. אם מישהו
(כולל אני, אחרי compaction) יתהה למה — **זו החלטה, לא חוב טכני**:

הפורמטים שמוגדרים ל-Lulu (`classic`, `hardcover`, `booklet`) הם פורמטים
של **הוצאה לאור** — נייר טקסט, כריכה דבוקה, לא lay-flat. ספר ברכות עם
תמונות דורש מעבדת צילום. בנוסף Lulu מדפיסה בחו"ל: משלוח, זמן ומכס.

נבדקו חלופות עם API (אוגוסט 2026): **Peecho** (lay-flat, 190gsm
E-Photo Lustre, HP Indigo — אבל בלי הדפסה בישראל, "שאר העולם" 10+ ימי
עבודה), **Gelato** (רשת 250+ שותפים; לא אומת שישראל ברשת),
**Cloudprinter**. **אף מעבדה ישראלית לא מפרסמת API** — Picabook,
albume, wow, myalbum, zooma, לופה כולן אתרי לקוח קצה.

**המסקנה:** צוואר הבקבוק הוא לא הספק אלא **שלב סידור העמודים בעורך של
Picabook**, שקיים רק כי נכנסים מדלת של לקוח פרטי. הצעד הבא הוא חשבון
עסקי מול Picabook עם הגשת **PDF מוכן לדפוס**. אם זה יקרה, זה נוגע
ישירות בסוגיית **היפוך סדר העמודים Lulu-מול-Picabook** שעדיין פתוחה.


## תבנית "חלון בצורה" (`template: 'window'`)

תמונה במלוא העמוד, ומעליה תמונת PNG/SVG עם **פתח שקוף** — כך שהצילום
נראה דרך קשת, עיגול, עלה, כל צורה שהאמנות פותחת.

**ההבחנה החשובה: כאן כן צריך שקיפות.** מסגרות התמונה במערכת עוברות
nine-slice ולכן **לא** צריכות אלפא — האמצע שם פשוט לא מצויר. כאן זה
הפוך: האמצע **כן** מצויר, מעל התמונה, והפתח חייב להיות שקוף באמת כדי
שהצילום ייראה מבעדו. JPEG לא יעבוד. PNG/WebP עם אלפא, או SVG.

**עמוד, לא משבצת.** התבנית מתעלמת במכוון מריפוד העמוד: כל הרעיון הוא
שהתמונה רצה עד קצה החיתוך והאמנות היא זו שמגדירה איפה העמוד נגמר. רקע
הפריסט עדיין נצבע מתחת, כך שאוברליי עם קצוות רכים מתמזג לספר.

**הטקסט לא כאן.** בעמוד מפוצל הברכה יושבת בעמוד הנגדי — זו ההתנהגות
הקיימת של `autoSplit`, ו-`resolveActiveTemplate` כבר מנתב נכון: עמוד
עם תמונה מקבל את התבנית הראשית, עמוד ברכה בלבד מקבל את
`blessingTemplate`. שם האורח כבוי כברירת מחדל (`windowShowName`) —
הוא כבר מופיע ליד הברכה, ולהדפיס אותו פעמיים גורם לכפולה להיראות
כמו טעות.

**האוברליי ב-`objectFit: fill`** ולא `cover`: האמנות נכתבת ליחס העמוד
וקצוותיה חייבים לנחות על קצוות העמוד. `cover` היה חותך את הצורה
מהמרכז ברגע שפורמט הספר משתנה.


### שני סוגי אמנות מועלית, ולמה רק לאחת נדרשת שקיפות

| | איפה מצויר | שקיפות |
|---|---|---|
| **מסגרת תמונה** (nine-slice) | סביב התמונה; האמצע **לא מצויר** | **לא נדרשת** — גם JPG |
| **צורת חלון** (`template: 'window'`) | **מעל** התמונה במלוא העמוד | **נדרשת** — הפתח חייב להיות שקוף |

**פער שתוקן:** ה-nine-slice נשלח עם ההבטחה "כל תמונה, בלי שקיפות",
אבל `uploadPhotoFrameAsset` עדיין דחה כל דבר שאינו PNG/WebP/SVG עם
ההודעה "מסגרת חייבת רקע שקוף". כלומר מה שהוכרז כאפשרי לא היה אפשרי,
דיאלוג אחד קודם. JPEG מתקבל עכשיו (ממילא הוא מקודד מחדש ל-PNG בדרך
פנימה), וכל בורר אומר את הדרישה שלו.

שני הסוגים חולקים את אותה ספריית נכסים בכוונה — למי שמנהל אותם זה אותו
דבר, ושתי ספריות היו אומרות שני מקומות לחפש בהם את הקובץ שהעלית.


### הטופס בעיצובים הממוסגרים "קפץ מהמעמד" — אנימציית CSS דרסה את המיקום

**התסמין:** בעיצוב ערב/בוקר הכותרת מתחילה במרכז המעמד במקום להיות
ממורכזת עליו, וכרטיס הטופס תלוי מלמטה-ימין וחתוך על ידי החלון. נראה
כמו באג בתצוגה המקדימה של הסטודיו — זה היה גם בדף החי.

**הסיבה:** הטופס ממוקם ב-`transform` **inline**:
`translate(-50%,-50%) scale(fit)`. על אותו אלמנט ישבה גם המחלקה
`animate-scaleIn`, שה-keyframes שלה מנפישים `transform`. **אנימציית CSS
גוברת על סגנון inline** — ועם `forwards` היא ממשיכה לגבור גם אחרי
שהאנימציה נגמרה. האלמנט נתקע לנצח על ה-keyframe האחרון,
`transform: scale(1)`: בלי translate, בלי scale.

**ומהצד השני:** בלוק `prefers-reduced-motion` מאפס
`transform: none !important` על אותה מחלקה — כלומר בדיוק למי שביקש
פחות תנועה, המיקום היה נמחק לגמרי.

**התיקון:** `animate-framedIn` — אותה כניסה, **אטימות בלבד**. באלמנט
הזה ה-transform אינו קישוט אלא **המיקום עצמו**. הכלל: אלמנט שממוקם
ב-transform inline לא לובש אנימציה שנוגעת ב-transform, וגם לא נכנס
לרשימת ה-reset של reduced-motion.
`tests/framedEntrance.test.js` שומר על שני הצדדים.
